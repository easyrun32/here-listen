version: 2
jobs:
  build:
    working_directory: ~/here-listen/
    docker:
      - image: circleci/node:15.2.0
    steps:
      - checkout
      - run:
          command: cd users && ls
      - run:
          command: cd users && npm install && npm test

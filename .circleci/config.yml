version: 2
jobs:
  build:
    working_directory: ~/here-listen/
    docker:
      - image: circleci/node:8
    steps:
      - checkout
      - run:
          command: pwd
          working_directory: /users
